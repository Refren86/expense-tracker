import styled from 'styled-components';

export const ExpensesWrapper = styled.div`
  max-width: 900px;
  padding: 1rem 1.5rem;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  row-gap: .5rem;
`